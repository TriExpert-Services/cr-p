# TriExpert Credit - Variables de Entorno (EJEMPLO)
# Copia este archivo a .env.local y configura tus valores

# Webhook URL para n8n (requerido)
WEBHOOK_URL=https://tu-instancia-n8n.com/webhook/tu-webhook

# Entorno (development | production)
NODE_ENV=development

# URL base de la aplicacion
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Rate limiting (requests por minuto por IP)
RATE_LIMIT_MAX=10
RATE_LIMIT_WINDOW_MS=60000
